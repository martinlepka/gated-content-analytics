# Supabase GTM Project
# Same project as main GTM app
NEXT_PUBLIC_SUPABASE_URL=https://jhglcgljsporzelhsvvz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Note: This app only needs the anon key for read-only access
# The gated-content-api edge function uses the service role key server-side
